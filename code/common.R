# %% Define ranges
forget_regret_rng <- c(-12, 0)
common_range_fs_thresh <- c(-15, 0) # used for fixed_share, thresholds
common_range_gamma_mu_sig_tail <- c(-1, 1)
lambda_range <- c(-5, 15)
sm_nonc_range <- c(-3, 3)
# %%

# %%
load(paste0(folder_res, "naive_err.rds"))
# %%
